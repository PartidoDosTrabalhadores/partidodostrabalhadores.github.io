if (window.location.href.indexOf("sorteio") != -1) {
    let divSorteio = document.getElementById("divSorteio")
    divSorteio.className = "divSorteio"
    divSorteio.innerHTML = "<h2>Sorteio</h2><span>Em Breve</span>";
}